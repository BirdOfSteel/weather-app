import React from 'react';

export default function HourlyTemperatures({ hourlyWeatherData }) {
    console.log(hourlyWeatherData)

    return (
        <div id="hourly-temperatures-div">
            <p>test</p>
        </div>
    )
}