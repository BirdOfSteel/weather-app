import React from 'react';
import menuIcon from '../assets/menu-icon.svg';

export default function Header({ isMenuOpen, setIsMenuOpen }) {

    return (
        <div id="header-div">
            <p style={{'margin':'0 auto'}}>UNDER DEVELOPMENT</p>
        </div>
    )
}